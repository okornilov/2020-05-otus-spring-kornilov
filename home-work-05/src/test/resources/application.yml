spring:
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-test.xml
  datasource:
    url: jdbc:h2:mem:librarydb
  shell:
    interactive:
      enabled: false
  messages:
    basename: messages
locale: ru